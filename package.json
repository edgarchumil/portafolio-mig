{
  "name": "portafolio-migdalia",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Portafolio profesional de Migdalia con HTML, CSS y JavaScript separados.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "build": "node scripts/build.mjs",
    "check": "node --check js/main.js && node scripts/build.mjs"
  }
}
